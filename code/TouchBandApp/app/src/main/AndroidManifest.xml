<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.toronto.touchband.touchbandapp">

    <uses-feature android:name="android.hardware.type.watch" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <uses-library android:name="com.google.android.wearable" android:required="false" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TaskActivity"
            android:label="task_activity">
        </activity>
        <activity
            android:name=".ScrollingStartActivity"
            android:label="scroll_start_activity">
        </activity>
        <activity
            android:name=".ScrollingActivity"
            android:label="scroll_activity">
        </activity>
        <activity
            android:name=".ZoomingStartActivity"
            android:label="scroll_start_activity">
        </activity>
        <activity
            android:name=".ZoomingActivity"
            android:label="zooming_activity">
        </activity>
    </application>

</manifest>